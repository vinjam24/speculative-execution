#include "Cache.h"
// template<typename K, typename V>
Cache::Cache(size_t capacity) {}

// template<typename K, typename V>
int Cache::getX(){
    return x;
}

// template<typename K, typename V>
void Cache::setX(int value){
    x = value;
}

Cache::~Cache(){}